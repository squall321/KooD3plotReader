# 전수 조사 확정 결함 (2026-09-17, 에이전트 154개)

원본: [findings.json](findings.json) — 각 항목의 메커니즘·시나리오·소비처·검증 의견 전문.

확정 60건 / 기각 3건 / 미검증 낮음 23건

## G1-cpp-core (9건)

| 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|
| high | `src/parsers/NARBSParser.cpp:79` | NARBS user-ID arrays read in the wrong order: shell, thick-shell and beam element IDs are shifted | Read the ID blocks in spec order: nodes, solids, beams, shells, thick shells. Better still, seek to each block with the header pointers (NSRH/NSRB/NSRS/NSRT). A |
| high | `src/analysis/SurfaceExtractor.cpp:99` | Exterior-face detection breaks on tets stored as degenerate hex8: interior tets land in the ±Z surface results and real boundary faces are missed | Build faces from each element's unique-node topology (tet: 4 triangles, wedge: 2 triangles + 3 quads, pyramid, hex). Hash triangles by their 3 sorted unique nod |
| medium | `include/kood3plot/analysis/AnalysisResult.hpp:485` | NaN/Inf are written as bare nan/inf in every JSON section except hotspot_clusters, which makes the whole analysis_result.json unparseable | Route every double written into analysis_result.json through jnum() (non-finite becomes null, significant-digit formatting), including timePointToJSON, surfaceT |
| medium | `include/kood3plot/analysis/AnalysisResult.hpp:512` | JSON histories, global_max/min, tensors and surface strain use fixed(8): strains below 5e-9 become 0 and 1e-7-scale peaks keep only 1-2 significant digits | Serialize with jnum() (setprecision(10), defaultfloat) instead of std::fixed<<setprecision(8) for all value and time fields in partStatsToJSON, timePointToJSON, |
| medium | `include/kood3plot/analysis/AnalysisTypes.hpp:758` | element_quality per-point data[] drops the aspect/volume/skew/warp measured flags, so the deep report plots AR=0 and volume ratio=1.0 for unmeasured parts | Serialize ar_measured/vol_measured/skew_measured/warp_measured per point (or write null via jnum when unmeasured) and have the charts skip unmeasured points. |
| medium | `src/analysis/UnifiedAnalyzer.cpp:1646` | Element quality looks at only 10 evenly spaced states, so transient inversions and peak distortion are missed | Evaluate every state, or at least track per-element extremes over all states (the states are already in memory). If sampling stays, record the sampled state cou |
| medium | `src/analysis/UnifiedAnalyzer.cpp:1094` | Segment-set parent-element lookup sends connectivity values (internal 1-based indices) through the user-node-ID map | Build node2elem with `ni = nid - 1` (internal index, range-checked). Keep ridx only for the segment's user node IDs. |
| medium | `src/analysis/SinglePassAnalyzer.cpp:862` | Deleted (eroded) elements enter all per-part and surface statistics as real zeros; StateData::deleted_solids is parsed but never read | Build a per-state deleted mask from state.deleted_solids (1-based array order) and skip those elements in min/avg/count, surface faces, cluster means and qualit |
| medium | `src/analysis/UnifiedConfigParser.cpp:152` | Part-name lookup reads an arbitrary .k file, ignores *INCLUDE and *PART_ variants; a zero-match part_pattern on surface jobs turns into the whole model | Resolve names from the main deck, following *INCLUDE, and accept *PART_* variants and multi-part *PART blocks. Treat a non-empty pattern that matches 0 parts as |

## G2-cpp-output (7건)

| 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|
| high | `examples/unified_analyzer.cpp:49` | Time column of stress/strain/motion CSVs written with std::fixed setprecision(6): µs-resolution times are quantized, duplicated, or collapse | Write the time column with std::scientific (or defaultfloat) and setprecision(10) in all four writers, matching writeSurfaceStrainCSV. Bump UA_OUTPUT_SCHEMA so  |
| medium | `examples/unified_analyzer.cpp:49` | Per-part stress/strain/beam CSV and motion CSV use std::fixed<<setprecision(6): strains below 5e-7 become 0.000000 and close state times merge | Write CSV numbers as significant digits, not fixed decimals (for example `std::defaultfloat << std::setprecision(10)` or `std::scientific << setprecision(9)`),  |
| medium | `examples/unified_analyzer.cpp:526` | Custom-report run into the documented shared folder overwrites deep's analysis_result.json, wiping motion_analysis / element_quality / hotspot_clusters | Have koo_custom_report pass `json: false` (it only reads metrics.json) or use a subfolder. Alternatively, have the C++ side merge into or refuse to overwrite an |
| medium | `examples/unified_analyzer.cpp:49` | All part, motion, surface and quality CSVs are written with std::fixed setprecision(6); fine time steps and small strains/displacements collapse | Write with std::scientific or std::defaultfloat and setprecision(9) (max_digits10 for float data), as the set-report CSVs already do. |
| low | `examples/unified_analyzer.cpp:608` | --skip-existing treats bare existence of analysis_result.json as 'completed' | Write a completion marker last (after renders and metadata), carrying tool_commit and a config hash. Compare that marker and the d3plot mtime in isAnalysisCompl |
| low | `examples/unified_analyzer.cpp:583` | Recursive result folder name: symlinked runs escape the output root, and 'case 1' / 'case_1' collide | Build names from the non-canonical path relative to root, or reject/encode '..' components. Make the name injective (escape '_' or keep spaces), and detect dupl |
| low | `examples/unified_analyzer.cpp:558` | findD3plotDirectories returns a partial list on a scan error and the batch still reports success | Use error_code iteration and skip only the failing entry, dedupe by canonical path, and exit non-zero (or list skipped subtrees) when the scan is incomplete. |

## G3-deep (13건)

| 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|
| high | `python/koo_deep_report/koo_deep_report/report/models.py:108` | peak_element_id comes from the 20-point truncated JSON, so it names an element from a different time step | Have unified_analyzer write the element id at the global max (e.g. 'max_element_id_at_peak') next to global_max/time_of_max and read that. At minimum, return No |
| high | `python/koo_deep_report/koo_deep_report/report/html_report.py:679` | Shell and thick-shell parts appear with peak von Mises 0.00 MPa, and the 'solid only' label shows only when hotspot clustering ran | Use None for parts that have no stress series and render them as '-' / 'not computed (shell)'. Derive the solid-only qualifier from the part element types in me |
| high | `python/koo_deep_report/koo_deep_report/report/html_report.py:326` | _downsample stride-decimates rcforc, motion, glstat, sleout and peak-tensor series to 500 points and loses spikes | Use min/max-preserving decimation (per bucket, keep the argmin and argmax, or LTTB plus forced global extrema) with a shared index set per series group. For ten |
| high | `python/koo_deep_report/koo_deep_report/report/html_report.py:295` | matsum chart: time axis is decimated but the [n_times][n_parts] energy rows are not, so energy is plotted at the wrong times | Decimate along the time axis with one index set: `idx = indices(len(t)); t=[t[i] for i in idx]; ie=[internal_energy[i] for i in idx]` (or transpose to per-part  |
| high | `python/koo_deep_report/koo_deep_report/report/html_report.py:560` | JS fmt() uses fixed toFixed(2/4/5), so s-unit times, GPa stresses, small strains and contact impulses collapse to 0.00 or identical strings; unit labels are hard-coded MPa/mm | Format with significant digits, e.g. `Number(v).toPrecision(4)`, switching to exponent notation for /v/ < 1e-3 (like hsMetricFmt or ft). Take the unit label fro |
| high | `python/koo_deep_report/koo_deep_report/report/models.py:135` | 'Peak displacement' is the max of the part-average displacement; the available Max_Disp_Mag column is ignored | Report max(Max_Disp_Mag) as 'peak displacement' (with its node id), and label the average-based value explicitly as 'peak average displacement' if it is kept. |
| medium | `python/koo_deep_report/koo_deep_report/report/batch_report.py:520` | Batch table 'T end' uses toFixed(1), so every simulation in seconds shows 0.0; strain uses toFixed(4) | Use toPrecision or exponent formatting for t_end and strain (e.g. `Number(v).toPrecision(4)`). |
| medium | `python/koo_deep_report/koo_deep_report/report/batch_report.py:621` | Batch part comparison keys parts by name, so parts that share a name overwrite each other within a case | Key by PID (or `${pid}/${name}`) and show the name as a label. Keep duplicate names as separate rows. |
| medium | `python/koo_deep_report/koo_deep_report/__main__.py:1085` | Batch output dir is output_root/<leaf dir name>, so sims with the same leaf name overwrite each other and one silently disappears | Name case_out from the path relative to the batch root (e.g. 'Test_A__output__Run_001'), or detect duplicates and add a suffix, and warn. |
| medium | `python/koo_deep_report/koo_deep_report/report/models.py:73` | has_mass_added is always False when glstat 'added mass' starts at 0, so mass-scaling warnings are suppressed | Parse 'percentage increase' (binout glstat 'percent_increase') and flag when it is > 1%, or compare added mass with the initial physical mass. Do not return Fal |
| medium | `python/koo_deep_report/koo_deep_report/core/keyword_parser.py:235` | The MAT field map reads the wrong card fields: yield from PR/HR/VP/C, FAIL from BETA/NUMINT | Fix the card/field table per the LS-DYNA manual (MAT_015 A is card 2 field 0; MAT_003 FS is card 2 field 2; MAT_098 A is card 2 field 0; MAT_124 SIGY is on card |
| medium | `python/koo_deep_report/koo_deep_report/core/keyword_parser.py:285` | Keyword parser drops every part after the first in a multi-part *PART block, all *PART_xxx variants and all *INCLUDE files; the impact loader then analyses only the surviving part IDs | Loop over title/card pairs until the next '*' line, parse *PART_* variants (skipping their extra cards), follow *INCLUDE relative to the deck. In the impact loa |
| medium | `python/koo_deep_report/koo_deep_report/core/energy_flow_builder.py:408` | Energy-flow edges: engage index defaults to 0 when a short pulse falls between decimated samples, and impulse/force are rounded to 4 decimals | Compute first_engage time on the full-resolution rt/fm (as contact_metrics does), store the time itself, and use None when nothing crosses thr. Resample with bu |

## G4-impact (16건)

| 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|
| high | `python/koo_impact_report/koo_impact_report/report/payload/physics.py:109` | Motion CSV Avg_Disp_*/Max_Disp_Mag became displacement (0 at t=0), but the impact report still reads them as part centroid position and impactor radius | Stop inferring geometry from motion CSV row 0. Either add explicit Centroid0_X/Y/Z and BoundingRadius0 columns (or JSON fields) in writeMotionCSV, or compute pa |
| high | `python/koo_impact_report/koo_impact_report/loader.py:1204` | stress_ts is built from the 20-sample truncated analysis_result.json arrays and plotted as the real stress history; the peak is missing | Treat `len(data) < num_points` (or any non-dict entry) as truncated: load stress/part_<id>_von_mises.csv, or set stress_ts=None with a load_issue. Do not silent |
| high | `python/koo_impact_report/koo_impact_report/loader.py:1722` | Parts or runs with no motion CSV or stress history are emitted as measured peak_g/peak_disp/peak_stress = 0, and a failed unified_analyzer run becomes an all-zero position with no load_issue | Use None, not 0.0, for parts without motion/stress (the loader already does this for s1/s3/e1/e3/evm). When load_per_part_motions returns no motions, record a ' |
| high | `python/koo_impact_report/koo_impact_report/loader.py:912` | rcforc contact mask uses a per-interface relative threshold, so noise-only interfaces mark 'contact' and set t_first_contact on runs where nothing touched | Apply a global floor, e.g. relative to the maximum interface peak across all interfaces or to impactor KE/v0 scale. Consider only interfaces that involve the im |
| high | `python/koo_impact_report/koo_impact_report/report/sections/s3_verdict.py:469` | Verdict table strain column uses toFixed(4), so realistic device strains of 1e-5 display as 0.0000 | Use the magnitude-aware fmt(), or toExponential as in s9_position.py:292, and in analyzer use `{:.3g}`. |
| high | `python/koo_impact_report/koo_impact_report/report/sections/s4_part_g.py:290` | Contact/peak times are multiplied by 1000 (ms), rounded to 2 decimals, and labelled with the solver time unit 's' | Label as 'ms' only when the solver time unit is s, otherwise leave unscaled. Use toFixed(3) or toPrecision(3) so µs-scale contact times stay distinguishable. |
| medium | `python/koo_impact_report/koo_impact_report/loader.py:1707` | peak_stress silently falls back to matsum peak internal energy (mJ), which is then labelled and compared as stress (MPa) | Remove the proxy, or store it in a separate field (e.g. peak_ie) with its own unit. Leave peak_stress None when no stress history exists. |
| medium | `python/koo_impact_report/koo_impact_report/report/payload/__init__.py:594` | A single global t_first_contact taken from the first trajectory is drawn as the 'impactor first contact' marker for every position | Emit t_first_contact per pos_id (e.g. part_motion.t_first_contact_by_pos) and look it up with the selected posId in s9 and s4. Exclude no-contact trajectories. |
| medium | `python/koo_impact_report/koo_impact_report/loader.py:1082` | Reuse of existing deep_report output has no staleness check, and the .impact_cache fingerprint ignores the unified_analyzer and loader version | Reuse only when analysis_result.json mtime >= d3plot mtime and the JSON is not truncated (num_points == len(data)). Include the unified_analyzer binary identity |
| medium | `python/koo_impact_report/koo_impact_report/loader.py:1141` | Motion CSV Time is written with fixed 6 decimals (1 µs); with output intervals below 1 µs the loader gets duplicate timestamps, and FFT/SRS/t_peak are off by the interval ratio | Write Time with std::scientific/setprecision(9), as the surface CSV already does at line 175. In the loader, detect non-strictly-increasing times and flag them  |
| medium | `python/koo_impact_report/koo_impact_report/report/payload/analytics.py:1028` | Impactor density is rounded to 6 decimals, so the ton/mm³ density shows as 0 in the part drill-down material card | Use _r4 (significant figures) or no rounding for density. |
| medium | `python/koo_impact_report/koo_impact_report/report/payload/__init__.py:313` | KPI worst_s is rounded to 1 decimal, so GPa-unit stresses collapse and the stress-limit comparison uses the rounded value | Keep full precision in the payload (or use _r4) and format only in JS. |
| medium | `python/koo_impact_report/koo_impact_report/report/payload/insights.py:187` | Damage Index reads yield via getattr on a dict, so yield-based DI is never used and the panel silently shows the composite score | Use `(sim_params or {}).get("yield_stress_by_part")` and `_pid_cast(peak_pos)` for the pos id. |
| medium | `python/koo_impact_report/koo_impact_report/loader.py:1987` | DOE discovery silently skips runs without step_config.txt or without d3plot/deep output, and the report never knows they existed | Record skipped Run_* dirs as load_issues (kind 'run-skipped' with reason) and count them in n_runs/n_failed. Warn when discovered positions != grid nx*ny. |
| medium | `python/koo_impact_report/koo_impact_report/report/payload/__init__.py:242` | Tier C (101-300 positions) keeps energy flows and acceleration curves for only the top 40 positions with no chunks and no notice; the energy panel silently keeps the previous position | Write chunks for tier C too, or clear the panel with an explicit 'dropped by tier cap (top-40)' message. Add the cap to the payload meta and show it. |
| medium | `python/koo_impact_report/koo_impact_report/report/sections/s5_doe.py:708` | Contact-sequence timeline hard-codes a 0-1 ms time axis and samples only 21 indices of the contact mask | Derive labels and cell times from tr.t (t0..t_end in the declared unit). Mark a cell engaged if any sample in its time bin is engaged (OR over the bin) instead  |

## G5-sphere-federate-scatter-custom (13건)

| 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|
| high | `python/koo_sphere_report/koo_sphere_report/report/html_report.py:194` | Sphere HTML time series are stride-decimated twice, and JS KPIs (peak G, pulse, crack arrest, hotspot element) are computed from what is left | Decimate with peak preservation (per-bucket min/max, always including the true-peak and true-min indices), or compute the pulse, CAI and hotspot-element metrics |
| high | `python/koo_sphere_report/koo_sphere_report/models.py:235` | σ3 minimum, ε3 minimum and peak_vel come from loader-decimated series (no true-min is captured) | In _load_stress_strain_csv, also capture true_min/true_min_time over all_min before decimation (and true peak /vel/ in _load_motion_csv). Make min_principal / m |
| high | `python/koo_sphere_report/koo_sphere_report/report/html_report.py:146` | Sphere HTML payload rounds stress/strain/disp to 1-2 / 4-6 decimals, so GPa stresses and small strains collapse | Round to significant figures, not fixed decimals (e.g. float(f'{v:.4g}')), or skip rounding and let gzip/JSON size tiers handle payload. In JS, format with toPr |
| high | `python/koo_federate_report/koo_federate_report/adapters/sphere.py:93` | Federate takes max() for σ3/ε3 (negative, compressive), so the 'worst' is the least-compressive value and deltas invert | Use one helper, worse(v, cur, metric) = v < cur for METRIC_COMPRESSIVE, else v > cur, in the sphere adapter aggregation, compare part worst / true peak / worst_ |
| high | `python/koo_federate_report/koo_federate_report/report/html_report.py:1209` | Federate HTML divides every metric by the acceleration G divisor and prints 0 decimals, so stress and strain show as '0' | Apply g_divisor only when DATA.metric === 'g' (and send the metric's own unit label). Choose decimals by magnitude (toPrecision(4) or exponent for /v/<1). |
| high | `python/koo_scatter_report/koo_scatter_report/charts.py:169` | Scatter direction map fixes pitch axis to ±90° while production DOEs use pitch ±180°, so about 40% of runs are drawn outside the SVG | Use the convention the sphere loader auto-detects (lat=roll ±90, lon=pitch ±180, swapped when max/roll/>91), or derive axis ranges from the data. Clip or flag o |
| high | `python/koo_sphere_report/koo_sphere_report/models.py:207` | Missing stress/strain/motion CSV is reported as a measured 0 (peak_stress/peak_strain/peak_g/peak_disp) | Return None when the series is absent (as peak_principal already does), omit the key in json_report/html payload, and let JS/federate show '—' / no_metric. |
| medium | `python/koo_sphere_report/koo_sphere_report/report/json_report.py:76` | report.json (federate sidecar) rounds peak stress to 2 decimals, strain to 6 and disp to 3; series stress_ts to 1 decimal | Write full-precision floats (or significant-figure rounding such as float(f'{v:.6g}')) for scalar peaks. If size matters, only the time-series arrays need compa |
| medium | `python/koo_federate_report/koo_federate_report/resample.py:373` | Sphere resample pairs cells by index-based angle name (P0200) as 'exact measured' even when the directions differ by up to 102° | For sphere, define an 'exact' match by angular distance (<= _ANGLE_EXACT_DEG between roll/pitch/yaw vectors), not by name. At minimum, verify that name-matched  |
| medium | `python/koo_federate_report/koo_federate_report/resample.py:279` | IDW resample interpolates only g/s/e/d, so s1/s3/e1/e3/evm become None and every interpolated cell is gated as '값 없음' | Iterate over models.METRIC_KEYS in _idw_sample. For compressive metrics interpolate the signed value; do not report interpolation gaps as '값 없음'. |
| medium | `python/koo_sphere_report/koo_sphere_report/from_json.py:15` | --from-json regeneration fabricates 2-point waveforms, drops σ1/σ3/ε/energy, and uses the stress peak time for strain | Load the stored stress_ts/strain_ts/g_ts/disp_ts, principal, strain and energy keys. If a series is absent, leave it empty and hide the dependent sections inste |
| medium | `python/koo_sphere_report/koo_sphere_report/loader.py:171` | Sphere unit detection maps every ms-based deck to ton-mm-s (or keeps it as default), so peak G is 1e6× too small | Disambiguate time/mass units with more than density (e.g. analysis_result.json end_time magnitude or the deck's *CONTROL_TERMINATION, impact velocity from drop  |
| medium | `python/koo_federate_report/koo_federate_report/adapters/sphere.py:137` | Sphere sidecars get hard-coded MPa/mm/G unit labels, so the federate unit-mismatch guard can never fire for sphere revisions | Have koo_sphere_report json_report write unit_labels from the detected unit system (MotionData.UNIT_SYSTEM plus the impact presets). In the adapter, leave label |

## G6-scripts (2건)

| 심각도 | 위치 | 내용 | 제안 |
|---|---|---|---|
| medium | `scripts/post_analyze.sh:370` | UA_OUTPUT_SCHEMA stayed 2 through output-changing analyzer fixes; deep step and impact reuse check only file existence | Bump the schema on every change to analyzer output, or better, key freshness on metadata.tool_commit / an output-schema field written by the C++ side plus d3plo |
| medium | `scripts/post_analyze.sh:685` | post_analyze --deep-only (and IMPACT flat mode) uses `${_num_groups:-1}`, so multi-group runs share one deep_out and the second group is skipped | Compute the group count inside run_step_deep (or in a shared pre-scan) instead of defaulting to 1, and include sub_path in deep_out whenever more than one d3plo |


## 기각

- `src/analysis/UnifiedAnalyzerRender.cpp:599` — Per-part auto fringe range comes from von Mises on 5 sampled states, looks elements up by material index, and is applied whatever the fringe type
- `python/koo_deep_report/koo_deep_report/core/d3plot_reader.py:592` — Motion CSV parser turns non-finite or unparsable cells into 0.0, which then plots and ranks as a real zero displacement or velocity
- `python/koo_impact_report/koo_impact_report/loader.py:1138` — The impact motion CSV parser does not coerce inf/nan, so one non-finite sample makes peak_g inf, which _safe() emits as 0

## 미검증(낮음)

- `include/kood3plot/analysis/AnalysisTypes.hpp:806` — set_reports block in analysis_result.json embeds name/title/notes/note without escaping, and set peaks inherit a sticky 
- `include/kood3plot/analysis/AnalysisTypes.hpp:791` — beam_analysis and motion_analysis JSON summaries use fixed(6): peak times 1.5e-7 and 1.4e-7 both print as 0.000000, and 
- `src/analysis/SinglePassAnalyzer.cpp:409` — Stress histories are serialized with a hard-coded unit "MPa" regardless of deck units
- `include/kood3plot/analysis/AnalysisResult.hpp:491` — max_element_id/min_element_id written as 0 for series that never set them
- `src/analysis/MotionAnalyzer.cpp:223` — Max_Disp_Node_ID is internal 1-based node index, not the user node ID
- `include/kood3plot/analysis/AnalysisTypes.hpp:823` — Extended JSON set_reports writes notes/name/title unescaped; a multi-line ffmpeg error makes analysis_result.json invali
- `include/kood3plot/analysis/AnalysisTypes.hpp:744` — element_quality min_jacobian written fixed(4): small negative Jacobians print as '-0.0000' and fail the `< 0` critical c
- `include/kood3plot/analysis/AnalysisResult.hpp:485` — Time-series JSON (data points, global_max/min, time_of_max) uses fixed(8), so values below 5e-9 become 0
- `src/analysis/SinglePassAnalyzer.cpp:775` — Principal and strain CSVs carry element ID 0 as if real in one ID column
- `src/analysis/MotionAnalyzer.cpp:57` — Requested motion part that is not in the mesh produces an all-zero motion time series instead of an error
- `src/analysis/MotionAnalyzer.cpp:267` — Motion velocity at the first state and acceleration at the first two states are written as 0 instead of 'not available';
- `src/analysis/MotionAnalyzer.cpp:223` — Max_Disp_Node_ID is the internal 1-based index, not the user node ID
- `src/D3plotReader.cpp:309` — A family file that fails to open or parse silently truncates every later state; only stderr is warned
- `src/analysis/SinglePassAnalyzer.cpp:409` — Stress unit hard-coded as "MPa" regardless of deck units
- `include/kood3plot/analysis/AnalysisTypes.hpp:741` — Sticky std::fixed in toExtendedJSON: element-quality metrics at 4 decimals, surface-strain values at 8, beam peaks at 6
- `python/koo_deep_report/koo_deep_report/core/sim_detector.py:67` — Only binout0000 is passed to lasso; branches written to binout0001+ are silently missing and reported as 'not in deck'
- `python/koo_deep_report/koo_deep_report/report/batch_report.py:652` — Batch part table checks stress_warning === 'OVER', but the producer emits 'crit', so parts over the limit never get the 
- `python/koo_impact_report/koo_impact_report/report/sections/s10_set.py:75` — Set-report overlay formats stress with toFixed(2) under a hard-coded 'MPa', formats the range with toFixed(1), and uses 
- `python/koo_impact_report/koo_impact_report/report/payload/doe.py:688` — IDW predictor rounds measured x/y to 1 decimal and then interpolates from the rounded coordinates
- `python/koo_impact_report/koo_impact_report/report/payload/doe.py:342` — Correlation network evaluates only the top 12 parts by mean G but reports that 'all parts respond independently'; the DI
- `python/koo_federate_report/koo_federate_report/adapters/sphere.py:91` — Sphere adapter overwrites per-part metrics when two part IDs share a name (last pid wins)
- `python/koo_sphere_report/koo_sphere_report/loader.py:328` — DOE angle lookup keys on '.1f' strings, so -0.0 vs 0.0 misses and the fallback name ignores yaw and collides
- `python/koo_custom_report/koo_custom_report/report/html_report.py:90` — Custom report peak table formats stress with '.2f' and peak time with '.6f'
